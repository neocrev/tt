[project]
name = "tt"
version = "0.1.0"
description = "Simple command-line time tracker — start, stop, log tasks with JSON persistence"
requires-python = ">=3.8"
authors = [{ name = "neocrev" }]

[project.scripts]
tt = "tt:main"
